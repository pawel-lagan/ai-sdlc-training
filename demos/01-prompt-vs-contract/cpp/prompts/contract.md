# Run B contract — C++

Work in Plan Mode. Do not edit files.

Goal: implement `task.md` in the existing deployment gate.

Context: inspect `include/deployment_gate.hpp`, `src/deployment_gate.cpp`, `tests/deployment_gate_test.cpp`, and `CMakeLists.txt`. Discover whether GCC or MSVC is active before proposing commands.

Constraints: preserve `GateDecision`; add no dependency or unrelated abstraction; do not change the C++ standard or warnings; keep the function pure.

The plan must name exact files, cases, order, focused verification, broad CTest verification, and non-goals. Cover low, medium, high production approved/unapproved, high non-production, missing/invalid risk, and existing behavior. Stop before editing.
