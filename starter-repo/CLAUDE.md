@AGENTS.md

# Claude Code adapter

Use `.claude/skills/` and `.claude/agents/` if this starter is adapted into native Claude Code artifacts. The canonical workshop artifacts are the shared `AGENTS.md`, the specs, and the skill content under `.agents/skills/`.
